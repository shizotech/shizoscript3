//Test whether variables assigned inside a brace-less if/else are visible outside.

pick(x)
{
	if(x < 0)
		res = [kind = "exit"];
	else
		res = [kind = "output"];

	return res.kind;
}

std.print(pick(-1), " ", pick(1), "\n");
