
Needs npx and python

Ensure these commands work on your system:

npx tsc
npx node
npx esbuild

If not install them (globally maybe)

npm install -g typescript node esbuild

Also install python selenium package

pip install selenium
