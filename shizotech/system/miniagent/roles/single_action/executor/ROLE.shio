

//Helper functions to set console output color
stdfcol(col) {
	std.color(col, "black");
}
stdbcol(col) {
	
	std.color("white", col);
}

add_tools(agt)
{
	agt.add_finalize_tool(true);

	agt.add_read_tools();
	agt.add_code_read_tools();
	agt.add_write_tools();
	agt.add_skill_tools();
}